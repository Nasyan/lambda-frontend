NEXT_PUBLIC_API_URL=http://localhost:8000

# APP_MODE can live in the shared ../.env, but browser-visible frontend values
# must be exposed with NEXT_PUBLIC_* in .env.local or next.config.
# NEXT_PUBLIC_APP_MODE=local
